{
  "name": "aegis-relay",
  "version": "0.1.0",
  "private": true,
  "description": "Privacy-first multi-server Emby reverse proxy panel",
  "type": "module",
  "engines": { "node": ">=20" },
  "scripts": {
    "start": "node src/server.js",
    "check": "node --check src/server.js && node --check src/security.js && node --check src/store.js && node --check src/proxy.js",
    "test": "node --test"
  },
  "license": "MIT"
}
