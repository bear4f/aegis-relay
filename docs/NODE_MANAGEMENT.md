# Emby 节点管理

## 路径与展示名称

短路径是客户端连接的唯一标识，允许小写字母、数字、`_` 和 `-`，最长 32 位。例如别名 `charity`：

```text
https://relay.example.com/charity/<访问密钥>/
```

展示名称只出现在管理面和可选的网关首页。首页不会展示密钥或上游地址。

访问密钥模式是默认值。密钥只显示一次，服务端仅保存带应用主密钥的 HMAC 摘要。仅别名模式适合另有 VPN、mTLS 或严格 IP allowlist 的环境，不建议直接暴露公网。

## 多线路与熔断

主线路可用分号或换行分隔，最多 8 条。每条线路有独立的内存健康状态：

1. 请求在未熔断线路间轮转。
2. GET/HEAD 遇到连接错误、超时、502、503 或 504 时尝试下一条。
3. 连续失败两次后熔断 15 秒；持续失败时退避时间逐级增加，最长 5 分钟。
4. 当所有线路均熔断时允许探测，成功后立即恢复。

非幂等请求不会自动重放，因为登录和 API 写操作可能产生副作用。

## 前后端分离

播放线路为空时，全部请求使用主线路。配置播放线路后，下列典型路径会切换到播放线路：

- `/Videos/` 与 `/emby/Videos/`
- `/Audio/` 与 `/emby/Audio/`
- `/LiveTV/` 与 `/emby/LiveTV/`
- `/Items/<id>/Download`

主线路和播放线路都使用相同的私网、TLS 和请求头安全策略。

## 302 直连与隐私中转

| 策略 | 行为 | 优点 | 风险/代价 |
|---|---|---|---|
| 客户端直连 | 将外部 Location 返回客户端 | 不消耗中转带宽，通常更快 | CDN/网盘可看到客户端 IP 与请求特征 |
| 隐私中转 | AegisRelay 在服务端跟随重定向 | 外部目标只看到代理出口 | 消耗代理流量，可能降低速度 |

隐私中转最多跟随 5 次 HTTP(S) 重定向。跨域后会删除 `Authorization`、`Cookie` 和 `X-Emby-Token`，并继续执行 DNS/IP SSRF 检查。

## 获授权的客户端兼容配置

请求头配置支持以下通用字段：

- `User-Agent`
- `X-Emby-Client`
- `X-Emby-Device-Name`
- `X-Emby-Device-Id`

这些字段用于自有或获授权服务器的兼容测试，例如统一设备名称或修复旧服务的 UA 识别。系统不修改用户认证 Token，也不提供面向第三方限制的冒充模板。若上游明确禁止反代或某类客户端，应尊重其规则并关闭该节点。
